README

  Fcrypt is a small command line tool for encrypting local files. This
  tool is meant to be a project for the Study Week Coding Camp
  Program, so it is not meant for any production use. This is a template
  to start the project.

GIT

  To download the latest source code off the GIT server, do this:

    git clone https://github.com/pako-23/fcrypt-template.git

  (you will get a directory named fcrypt-template created, filled with
  the source code)
